# Gold Analyzer (XAUUSD) — Project Context

Project: analyzer harian XAUUSD, serverless penuh (GitHub Actions + JSON di repo + static hosting).
Semua komunikasi dengan user dalam **Bahasa Indonesia**.

## ATURAN KERJA (WAJIB)
- **Commit/push ke GitHub HANYA kalau user menyuruh.** Jangan pernah commit/push atas inisiatif sendiri.
- **Credential tidak boleh bocor**: API key hanya di `.env` (gitignored) lokal dan GitHub Secrets. Tidak pernah hardcoded, tidak pernah di `data/`, frontend nol secret.
- **Notifikasi = Web Push ke PWA sendiri + WhatsApp via Fonnte** (Web Push: permintaan user 2026-09-09; WA Fonnte: 2026-09-20 — Telegram tetap TERLARANG, sudah dihapus total, jangan ditambahkan lagi). **Web Push**: `analyzer/push.py` + `pywebpush`, dipicu job daily. Notif: (1) setup entry yang TERCATAT feedback loop (1×/entry, dedup `created_at` — re-entry sehari sama tetap dapat notif; sinyal saat posisi masih aktif = senyap), (2) agenda event 3★ hari itu H-3 jam sebelum event pertama (1×/hari). Credential: `PUSH_VAPID_PRIVATE_KEY` + `PUSH_CONTACT_EMAIL` + `PUSH_SUBSCRIPTIONS` (JSON subscription) **hanya GitHub Secrets + `.env` lokal** — subscription TIDAK boleh di `data/` (repo public, endpoint bisa dipakai spam). Public key VAPID memang boleh di frontend (`VAPID_PUBLIC_KEY` di data.ts). Status pengiriman di `data/push_state.json` (`last_status: ok|expired|error`) → UI banner "notif putus". **WhatsApp Fonnte**: `analyzer/wa.py` (API POST `api.fonnte.com/send`, header `Authorization: token`, body `target`+`message` — doc Fonnte contohnya PHP tapi cukup `requests`), dipicu job daily step 6b, **cuma 2 fungsi persis seperti Web Push**: (1) pesan entry TERCATAT (dedup `created_at`, senyap saat posisi aktif), (2) agenda event 3★ hari itu H-3 jam sebelum event pertama (1×/hari WIB, isi = semua event hari itu; tanpa event 3★ hari itu = tidak ada pesan). Dedup state INDEPENDEN di `data/wa_state.json` (tanpa data sensitif — tanggal + status saja). Credential: `FONNTE_TOKEN` (token device Fonnte) + `FONNTE_TARGET` (nomor WA penerima, format 62xxxxxxxxxx) **hanya GitHub Secrets + `.env` lokal**; kosong = dilewati tanpa error; gagal kirim tidak pernah gagalkan job (notif = bonus).
- Waktu tampil selalu **WIB (Asia/Jakarta)**, penyimpanan UTC, timeframe H1/H4 (H4 prioritas).
- Berita hanya dari sumber wire/valid (whitelist), event fundamental 3★ US only (Trading Economics).

## Batch 5 LOKAL (2026-09-22, BELUM di-push): lantai SL 0.75×median ATR-100 (proteksi squeeze)
- **Konteks**: entry short pertama jalur inside_bar short (22 Sep 05:16 WIB, entry 4342.28, bar sinyal range $1.57 = 0.12×ATR saat ATR14 collapse $20→$13) kena SL 1 jam kemudian — bounce sesi Asia 2.17×ATR menyapu SL 1×ATR; TP1 justru tersentuh 7 jam kemudian (arah benar, kalah di squeeze-release). Ide limit order user (buy/sell limit di retrace) DIUJI & DITOLAK (skrip `scripts/audit_limit_entry.py`): inside_bar anjlok OOS +0.176→+0.028..0.085 (adverse selection — retrace fill = breakout gagal), frekuensi malah turun.
- **Lantai SL**: `SL = max(sl_atr×ATR14, 0.75×median ATR14 100 bar sebelumnya)` — SL tak ikut menyusut saat ATR collapse. Bukti 3 tahun konsisten PER TAHUN dua arah (inside_bar long +0.08/+0.13/+0.16/+0.13 → +0.17/+0.20/+0.21/+0.16; short membaik semua tahun). R nominal per win mengecil jujur (tp_dist/sl_dist). TIDAK menyelamatkan entry 22 Sep spesifik (rel_atr 0.814 > 0.75) — yang melewatkannya cuma filter bucket squeeze (n=32 belum cukup, watch item `scripts/audit_compression.py`).
- **Implementasi**: `backtest.SL_FLOOR_MED_MULT=0.75` + floor di `evaluate()` (sebelum cost-shift; kolom `atr_med100` di `indicators.add_indicators` = median shift-1 rolling 100); `recommend` level SL live rumus PERSIS sama + rationale "SL digenjangkan…" saat aktif + params `sl_floor_med`; `research` params patterns.json + note; teks Backtest panel UI; APP_VERSION 2.7 + APP_UPDATED_WIB 22 Sep 2026.
- **Efek gate pasca-regen (terverifikasi lokal)**: inside_bar long LOLOS (n=802, EV OOS +0.182), inside_bar short LOLOS (n=256, +0.199), **bullish_engulfing long GUGUR fail-closed (EV OOS −0.003, tadinya tipis +0.008)** — jalur resmi tinggal 2 (inside_bar long/short); jalur bull-engulfing-long belum pernah menghasilkan entry live sejak regen jadi dampak praktis nol. bearish_engulfing short tetap gugur.
- **Eksperimen anchor TF (pertanyaan user "perlu selain 4H/1H? takut telat", skrip `scripts/audit_anchor_speed.py`)**: anchor lebih CEPAT buruk — h4_ema20 (close vs EMA20) n naik +25% tapi EV turun (long OOS +0.182→+0.119); h4_cross (tanpa slope) ≈ sama persis dgn produksi (slope konfirmasi bukan sumber "telat"). Menarik: anchor lebih LAMBAT (D1 EMA20/50) long OOS +0.249 > H4 +0.182 — tapi jalur short D1 nyaris kosong (n=101, 2024–25 tanpa data, gold bull market D1 jarang down) → TIDAK diadopsi, dicatat watch item. Literatur: candlestick polos tidak menghasilkan nilai (Marshall-Young-Rose 2006 JBF), trend/momentum berlaku lintas futures dgn lookback ≤12 bln + vol-scaling (Moskowitz-Ooi-Pedersen 2012 JFE) → pola PERLU konteks trend TF besar + sizing volatilitas (ATR) — struktur sistem sudah selaras literatur.
- Verifikasi: SEMUA suite lulus (10 file + test_slfloor baru 4 tes), tsc + build lulus. APP_VERSION 2.7.

## Batch 4 LOKAL (2026-09-21, BELUM di-push): cooldown re-entry 4j → 2j (audit frekuensi Tahap 2)
- **Konteks**: user minta sistem lebih peka momentum dengan risiko masih aman. Dua tuas diuji di data 3 tahun (metodologi identik produksi: searah H4 as-of, net cost $0.35, guard momentum, Wilson CI, OOS 30% per arah) — skrip `scripts/audit_freq_levers.py` + `scripts/audit_momentum.py` + `scripts/audit_today.py` LOKAL (jangan di-commit).
- **Tuas 1 — buka entry saat H4 sideways: DITOLAK data.** Pool sideways semua EV negatif OOS (inside_bar long −0,113R, short −0,318R; engulfing n_oos < 30 & inkonsisten per tahun). Filter H4 terbukti kembali menyelamatkan — H4 sideways TETAP tanpa arah, tidak ada dasar entry.
- **Tuas 2 — COOLDOWN 4 bar → 2 bar: LOLOS, diimplementasikan.** Dedup 2 bar: sinyal +27%, EV net OOS stabil/naik (inside_bar long +0,150R → +0,182R; short +0,230R → +0,186R); dedup 1 bar sudah terlalu bocor (short merosot, DD −44R). Biaya yang diterima user ("naik risiko asal masih aman"): DD long 11R → 16R, loss streak 9 → 14. Implementasi: `research.COOLDOWN_BARS=2` (kolam statistik gate = aturan live) + `daily.COOLDOWN_HOURS=2` + fallback `?? 2` Home.tsx (UI utama sudah baca `cooldown_bars` dari patterns.json params — otomatis) + tes cooldown diperketat (blok di 1h59, lolos di 2h30). patterns.json regenerasi otomatis saat run daily berikutnya (n naik ~27%, gate tetap fail-closed per pola-arah).
- **Kandidat momentum mom3 (3 close beruntun ≥1 ATR) DIUJI & DITOLAK** (permintaan awal user, penting jangan diuji ulang): standalone tampak kuat (long OOS +0,295R) TAPI kolam gabungan produksi meruntuhkannya — sinyal untungnya overlap klaster inside_bar/engulfing; varian fallback (sinyal benar-benar independen = satu-satunya yang menambah entry live karena cooldown blok yang overlap) EV OOS −0,008 long / −0,075 short, per-tahun inkonsisten. Juga teruji gagal: mom2 0.75 ATR, Donchian 20 (EV full +0,035 tipis), pullback EMA20 (mode aman OOS −0,098). Mode aman utk short = false safety (SL ketat 0,75×ATR dicukur volatilitas gold; batch 1: −0,178R vs −0,133R).
- **Audit Senin 21 Sep** (konteks keputusan): gold −$43 intraday, NOL pola sepanjang 22 slot (2 bearish_engulfing nyaris terjadi beda $0.03/$1.22), H4 flickering up/sideways sepanjang dump → semua slot netral jujur TANPA note gate; entry normal terakhir 12 Sep (cuma inside_bar long yang lolos gate sejak bullish_engulfing long gagal OOS −0,012 pasca regen). Jalur short live tetap inside_bar short (lolos gate OOS +0,055) — aktif begitu H4 flip down.
- Verifikasi: 58/58 Python lulus, tsc + build lulus. APP_VERSION 2.6 + APP_UPDATED_WIB 21 Sep 2026.

## Batch 3 TER-PUSH `d54f8e7`+`6c944ae` (2026-09-20/21): WhatsApp gateway Fonnte

- **`analyzer/wa.py`** — kanal notif kedua (tambahan Web Push), cuma 2 fungsi persis Web Push: (1) pesan entry TERCATAT (dedup `created_at`, senyap saat posisi aktif; re-entry sehari sama tetap dapat pesan), (2) agenda SEMUA event 3★ hari itu H-3 jam sebelum event pertama (1×/hari WIB; tanpa event = tanpa pesan). API Fonnte = POST `api.fonnte.com/send` (header `Authorization: token`, body `target`+`message`; doc PHP tapi cukup `requests`). Dedup state TERPISAH `data/wa_state.json` (`entry_notified_id` + `agenda_date` + `last_status`); id hanya dicatat saat kirim sukses → gagal = retry iterasi watch loop berikutnya. Credential `FONNTE_TOKEN` + `FONNTE_TARGET` (62xxxxxxxxxx) hanya Secrets + `.env`; kosong = skip tanpa error; tidak pernah gagalkan job. Reuse `push._events_today`/`AGENDA_LEAD_H`/label — tanpa duplikasi logika.
- Wiring: `daily.py` step 6b (try/except non-fatal persis push) + `daily.yml` env FONNTE_* + `tests/test_wa.py` 7 tes (isolasi tempdir: patch `store.DATA_DIR` + `push.DATA_DIR` — `_events_today` baca kalender dari situ — + `wa.STATE_FILE`). **Fix ikutan**: teks notif agenda `push.py` masih "−2j..+1j" (sisa blackout lama batch 2) → "−2j..+3j" + assertion test_push. APP_VERSION 2.5.
- **`.github/workflows/wa_test.yml` (dispatch only)**: kirim 2 pesan CONTOH untuk review copywriting via `wa._send` LANGSUNG (bukan dispatch → tidak menyentuh wa_state.json, dedup produksi aman); agenda pakai event hari itu kalau ada, kalau kosong contoh realistis. Aman dihapus kapan pun.
- **Live terverifikasi**: dispatch 17:12:48 UTC → iterasi pertama commit `4252e5d` 54 detik kemudian (netral + `stale:true` jujur; tanpa wa_state.json karena memang tak ada entry/event hari itu); run WA Preview `35525139297` completed success = kedua pesan contoh terkirim (credentials + Fonnte oke).

## Batch 2b TER-PUSH `57d1323` (2026-09-20): eksperimen dikeluarkan dari UI Riwayat
- **Keputusan user**: 13 entry eksperimen lama tidak perlu tampil lagi di UI. Implementasi: `RiwayatView.history` difilter `!h.experiment` (ledger, filter pola, hitungan "X dari Y" otomatis ikut), slot rec_log eksperimen difilter dari panel per hari, tile "Eksperimen" result-strip dihapus, badge ⚠ EKSPERIMEN dihapus (ledger, slot, Analysis dead-path: `rec.experiment` tak mungkin true lagi untuk rec baru — recommendation.json dioverwrite tiap run), teks jeda event "−2j..+1j" → "−2j..+3j". Tipe data.ts tetap punya field `experiment` (defensif — data repo lama masih memuatnya). Data TIDAK dihapus: tracking.json utuh, backend tetap pisah statistik.
- **`track.compute_stats`: `total` kini `len(main)` (entry normal saja)** — konsisten dengan UI yang menyembunyikan eksperimen (tile "Total tercatat"/"Rekam jejak sistem"/"rekomendasi tercatat total" semua pakai stats.total); test_p5 `test_stats_experiment_split` diupdate (total 5 → 2). Stats utama memang sebelumnya TIDAK memuat eksperimen di wins/hit-rate (hanya total yang menghitung semua) — hit-rate 50% = 3 win/6 entry normal, eksperimen 23% tetap terpisah di stats.experiment (data backend, tidak lagi ditampilkan UI).
- **Verifikasi**: test_p5 + test_audit + report smoke lulus, tsc + build lulus (201 kB gzip). APP_VERSION → 2.4. **Live terverifikasi**: run dispatch `35521813700` iterasi pertama `f780c4e` (16:10 UTC) → `stats.total: 6` (3 win / 2 SL / 1 timeout, hit-rate 50% utama tetap tanpa eksperimen; 13 eksperimen tetap di backend `stats.experiment`). CATATAN VERIFIKASI: watcher `git ls-remote` harus bandingkan HASH PENUH 40 karakter (banding vs hash pendek selalu ≠ → exit palsu); commit data di antara push dan run dispatch baru masih dari loop run lama (kode lama) — jangan baca stats dari commit pra-run-baru.

## Batch 2 TER-PUSH `a40d345` (2026-09-20): backtest kondisi event 3★ — arsip historis + blackout +3h + kartu versi UI + bersih-bersih PWA lama
- **Arsip event 3★ US historis** `data/events_hist.json` (821 event masa lalu 2023-09 → 2026-09, seed via `scripts/build_events_hist.py` LOKAL — paginasi `startdate` TE ~5 minggu/request, 37 request; event masa depan DIFILTER karena jadwal bisa bergeser). Dipertahankan otomatis: job fundamental tiap run memanggil `cal.archive_past(feed)` (idempotent per (t_utc, title); hanya event yang sudah lewat yang diarsip) — tanpa arsip ini analisa korelasi event mustahil karena feed kalender hanya punya event ke depan. Fungsi: `calendar.load_hist()` (urut waktu, korup/absen → [] fail-soft) + `calendar.archive_past()`.
- **Validasi blackout pakai data (scripts/audit_events.py LOKAL, 821 event × 18.938 bar H1 bersih)**: profil volatilitas |Δclose|/ATR14 bar rilis = **2,24x baseline**, +1h 1,72x, +2h 1,38x, **normal kembali di +3h** (pra-event cuma 0,9–1,2x = tenang). EV sinyal kolam produksi (dedup+aligned+net) per jarak ke event: **+0..+3h pasca-event −0,25R (n=118, win 30%)** vs jauh-dari-event +0,08R — jendela lama +1h masih melewatkan 2 jam termahal. Pra-event −2..0h justru +0,15R tapi blackout pra-event DIPERTAHANKAN 2 jam demi risiko ekor (slippage/gap menembus SL saat rilis; backtest R menganggap fill SL persis = optimis saat spike 2,24x ATR).
- **BLACKOUT_AFTER_H 1,0 → 3,0** (`calendar.py`, konstanta + komentar bukti): backend (`active_blackout` → status "tunggu"), `calendar.json blackout_hours` otomatis, frontend `BLACKOUT_AFTER_MS` data.ts (3h) + teks "−2h..+3h" Home.tsx — semua sinkron. Efek live: sinyal di +1..+3h pasca rilis yang tadinya boleh entry kini "tunggu" (~6% sinyal historis, EV-nya −0,3R). Negatifnya +3..+12h pasca-event yang lemah (−0,06R, n=265) TIDAK ditindak — bukti lemah vs noise per-jam-UTC (±0,3R) dan akan memblokir ~30% sinyal.
- **Tes** `tests/test_events_archive.py` (load_hist absen/korup/schema/urut; archive_past idempotent + skip future + skip invalid; jendela −2h..+3h) — **mem-patch `store.DATA_DIR` ke folder temp**; versi awal tes pernah MENGHAPUS arsip produksi (bug: `_reset()` unlink data/events_hist.json) → arsip di-rebuild; pelajaran: tes JANGAN pernah menulis file data/ produksi. `test_p4.py` test_blackout diupdate (+2h masih jeda, +3h1m selesai). Semua suite lulus + tsc + build (201 kB gzip).
- **Audit fungsi tertinggal (permintaan user)**: semua referensi tp2/experiment yang tersisa SENGAJA — data legacy di repo (field tp2 runtime-diabaikan), jalur UI/stats experiment untuk 13 entry historis (kompatibilitas), `_experiment_eligible` dipertahankan sebagai dokumentasi keputusan 2026-09-20.
- **PWA vanilla lama DIHAPUS (persetujuan user 2026-09-20)**: index.html/app.js/style.css/sw.js/manifest.webmanifest/icon.svg di root repo sudah `git rm` — tidak pernah ter-deploy (Vercel root=web); README struktur diupdate (web/ + events_hist.json).
- **Kartu versi sidebar (permintaan user 2026-09-20)**: kartu "Growth desk / Research workspace" (tak berfungsi) diganti **GoldPulse v{APP_VERSION} + "Update kode terakhir: {APP_UPDATED_WIB}"** — konstanta `APP_NAME/APP_VERSION/APP_UPDATED_WIB` di data.ts. **WAJIB di-update TIAP BATCH yang di-push** (v2.3 = batch 2; v2.2 = batch 1; v2.1 = docspec gate sadar-arah). ChevronDown import dihapus dari Home.tsx.

- **Verifikasi live (19:48 WIB 2026-09-20)**: iterasi pertama kode baru `9b712c0` — rekomendasi Minggu malam `netral` + `stale: true` jujur (bar 39 jam basi → tanpa entry phantom); `events_hist.json` 821 event live di repo; `calendar.json blackout_hours` masih `after:1.0` WAJAR (ditulis run fundamental pra-push, cuma info display — penegakan backend+frontend sudah 3 jam; run fundamental berikutnya menulis `after:3.0`).

## Batch 1 TER-PUSH `ac5ff6e` (2026-09-20): audit live 19 entry — 5 fix algoritma + TP1-only
- **Audit 19 entry**: semua outcome resolusi BENAR vs chart (SL-first konservatif, high/low intrabar) — sinkron dengan data OHLC. Yang bikin jelek: (a) **bar datar feed pasar tutup** (range ~$0,27; 9,1% bar H1 = 1.896/20.834) mengotori ATR14 (collapse 2,6–4,1 dolar) → 3 loss eksperimen weekend; (b) **re-entry beruntun < 1 jam** (revenge re-entry setelah SL — 3 loss dalam 3 jam) melanggar asumsi dedup 4 bar statistik gate; (c) 2 entry statistik utama = phantom (1 bar Minggu, 1 data basi); hit-rate eksperimen jujur ~30–36%. Entry real long tidak muncul 13–19 Sep karena memang NOL sinyal bullish + H4 sideways saat bounce (bukan karena keburukan eksperimen — keduanya pakai pool pola berbeda per arah).
- **#1a Filter bar datar**: `store.MIN_RANGE_USD=0.35` + `is_trading_bar()` — bar range < $0,35 dibuang di `store.load()`/`store.save()` (merge lewat load yang sudah difilter juga PURGE bar lama di file saat sync berikutnya); `gap_report` weekend-bridge (Jum/Min → Min/Sen ≤ 54 jam = normal, tanpa bar closure datar). research/patterns.json otomatis bersih saat regenerasi.
- **#1b Enhance algoritma — guard momentum-lawan (KHUSUS short, evidence-based)**: `recommend._counter_momentum` — sinyal short DIBLOK kalau mom3 = (close−close[3 bar lalu])/ATR14 > 0 (bounce). Bukti 3 tahun (kolam produksi: dedup 4 bar + searah H4 + net cost, TANPA bar datar): inside_bar short bounce −0,167R (n=165) vs searah −0,045R (n=198); bearish_engulfing short bounce −0,500R. LONG DIBALIK: inside_bar long dip +0,188R = subset TERBAIK → guard TIDAK simetris (long bebas). Semua 3 loss eksperimen after-Fed punya mom3 naik (+0,65/+1,33/+0,91) → guard menangkap kasus after-event tanpa perlu kalender historis. Sinyal terblokir → netral + `rec["gate"]["momentum_block"]=True` + rationale + slot note rec_log. Ekstensi "after-event window" TIDAK ditambahkan: kalender cuma punya event ke depan (tidak bisa di-backtest) — guard momentum adalah mekanismenya.
- **#1c Guard data basi**: `recommend.STALE_HOURS=3` — bar H1 closed terakhir > 3 jam → netral `rec["stale"]=True` (cegah entry phantom saat sync drop / pasar tutup lama).
- **#2 Cooldown re-entry live**: `daily.COOLDOWN_HOURS=4` + `apply_cooldown()` + `tracking["last_signal"]={"d","t"}` — sinyal entry SEARAH < 4 jam dari sinyal searah terakhir → status "tunggu" + flag `cooldown` (konsisten dedup 4 bar yang jadi dasar statistik gate; arah beda bebas). `last_signal` di-update oleh SETIAP sinyal yang lolos cooldown (termasuk yang tak jadi posisi karena 1-posisi-aktif — persis dedup research yang menghitung sinyal, bukan posisi); sinyal yang terblok cooldown TIDAK memperbarui. Baru mulai terasa setelah tracking punya last_signal (run pertama pasca-deploy bebas blok).
- **#3 Fix window resolusi track.py**: `after = bars t > rec["signal_bar_t"]` (bar SETELAH bar sinyal — persis backtest), fallback `created_at` untuk rec lama; dulu filter `created_at` (jam run, mis. 23:26) melewati bar PERTAMA posisi yang sedang berjalan saat run → MFE/MAE/bars_held meleset + outcome bisa salah. Timeout kini butuh `len(after) > horizon` (bar ke-24 pasti closed). `recommend` mencatat `signal_bar_t` di rec entry.
- **#4 UI Riwayat jujur**: slot entry `recorded` flag (reclog) → pill "Posisi dibuka" vs "Rekomendasi saja" (slate, ikon Crosshair + note); note label sadar-konteks (cooldown re-entry / data basi / disaring / jeda event 3★ / tanpa setup); **dedup slot identik < 60 menit** di reclog (grid :37 + cadangan :52 + watch loop pernah menulis 3–4 slot duplikat per jam; kriteria identik = status+bias+level entry+note, level beda atau > 1 jam tetap dicatat).
- **#5 EKSPERIMEN DIHENTIKAN (keputusan user 2026-09-20, menggantikan opsi 2+3 tahun 2026-09-12)**: arah dengan bukti per-arah bermakna (n ≥ 30) tapi EV arahnya negatif → **NETRAL fail-closed**, tidak pernah jadi posisi. `recommend._experiment_eligible` kini selalu return False (dipertahankan sebagai dokumentasi keputusan); loop gate cuma punya jalur qualified; rationale sinyal tersaring kini menyebut "eksperimen dihentikan". Hanya entry lolos gate (EV net per-arah positif) yang dieksekusi — praktisnya: **short live praktis tidak akan entry** sampai ada pola short yang terbukti EV-positif; histori 13 entry eksperimen lama TETAP terpisah di `tracking.stats["experiment"]` + badge EKSPERIMEN di UI (kompatibel histori). Bukti penentu: sisa kolam eksperimen pasca-fix = inside_bar short momentum-SEARAH −0,045R (n=198), tetap negatif.
- **TP1-ONLY (keputusan user 2026-09-20, "gaperlu TP2")**: TP2 tidak pernah memengaruhi statistik (feedback loop memang selalu resolve di TP1) — ini murni penyederhanaan: level yang tampil = level yang dinilai. Dihapus: `backtest.evaluate_tp2` + `TP2_ATR`, `recommend` levels/params/rationale tanpa tp2, `reclog` slot levels tanpa tp2, chip TP2 di UI (Overview/Analysis/Riwayat), `Levels` tipe data.ts tanpa tp2 (data lama di repo masih punya field tp2 — diabaikan runtime). PDF §5b "TP2 runner" diganti tabel **"Statistik risiko per pola"** (Max DD R + loss runtun dari rule standar; `backtest.risk_stats` dipertahankan). Data lama tracking/rec_log tetap tampil benar.
- Tes: `tests/test_audit_2026_09_20.py` (12 tes: flat filter+purge, weekend gap, stale, momentum blok/lolos/long-bebas, cooldown 3 kasus, window resolusi, timeout closed-bar, recorded flag, dedup slot, note cooldown/basi) + fixture bar test_p1 diberi range $2 + `test_direction_gate_and_experiment` (b) diubah: short EV-negatif → netral. SEMUA suite lulus (P1/P3/P4/P5/reclog/push/report smoke + audit baru), tsc + build lulus (bundle 201 kB gzip).

## Status (per 2026-09-06): SEMUA PHASE P1–P5 SELESAI & TERDEPLOY
- Repo: **github.com/ponimazino/gold** (public). Remote: `https://ponimazino@github.com/ponimazino/gold.git`.
- Data 3 tahun ter-backfill: 20.493 bar H1 + 5.323 bar H4 (2023-09-06 → 2026-09-06).
- Workflow GitHub Actions (checkout@v5, setup-python@v6, semua commit step pakai `git pull --rebase origin main` dulu untuk hindari race):
  - `daily.yml` — grid 1 jam "37 0-15 * * 1-5" + "37 21-23 * * 0-4" (04:37–22:37 WIB, Sen–Jum, ~19 slot/hari; anchor 04:37 pra-Sydney, 14:37 London, 20:37 NY + rilis data AS; tiap candle H1 baru dievaluasi ≤ 37 menit setelah close, tepat sekali sebagai bar terakhir — analisa hanya pakai candle CLOSED, `recommend._closed_only` membuang bar yang sedang berjalan) + **slot cadangan :52** → research + resolve feedback loop + `data/recommendation.json`, `data/tracking.json` + slot log per-run `data/rec_log.json` (`analyzer/reclog.py`: tiap run analisa dicatat jam WIB + status entry/tunggu/netral, dedup per menit, arsip 30 hari; slot entry yang tidak jadi posisi (posisi lain masih aktif) diberi `note` "tidak dicatat/dinilai feedback loop" — `entry_recorded=False` dari `daily.record_entry`; slot non-entry tersaring gate juga diberi `note`) + **Web Push** (`push.dispatch`: notif entry + agenda event; dedup per hari WIB di `data/push_state.json`; secrets kosong = dilewati tanpa error; gagal kirim tidak pernah gagalkan job). Recommendation.json dioverwrite tiap run; history tracking = **maksimal 1 posisi AKTIF** (aturan 2026-09-10: entry baru hanya dicatat kalau posisi terakhir sudah selesai TP1/SL/timeout — boleh re-entry sehari yang sama, tanpa overlap; kalau masih ada posisi aktif, sinyal tampil di recommendation.json dengan catatan rationale tapi tidak dinilai feedback loop — `daily.record_entry`). **Watch loop (2026-09-08)**: begitu satu run lolos, runner tetap hidup ~6 jam dan tiap 1 jam menjalankan **sync sendiri + analisa + commit** — tidak lagi bergantung workflow sync jalan duluan. `concurrency: daily, cancel-in-progress: true` (maks 1 loop aktif).
  - `sync.yml` — "17,47 * * * *" (setiap hari termasuk Minggu; :47 = cadangan anti-drop): update bar H1/H4, gap-filling. **Watch loop (2026-09-08)**: run yang lolos tetap hidup ~6 jam, sync + commit tiap 30 menit. `cancel-in-progress: true` (maks 1 loop, kuota Twelve Data terkendali ~24-48 req/run yang lolos).
  - `fundamental.yml` — "13 1-23/3 * * *" + cadangan ":28" (tiap 3 jam): kalender 3★ US + berita whitelist
  - `report.yml` — **Laporan Harian (EOD)**: "7 16 * * 1-5" + cadangan "53 16 * * 1-5" (23:07 WIB Sen–Jum, setelah slot grid daily terakhir 22:37 WIB) + dispatch: `analyzer/jobs/report.py` → PDF harian `data/reports/daily-YYYY-MM-DD.pdf` + `data/reports/index.json` (max 90 entri; reportlab di requirements.txt; smoke test `tests/test_report_smoke.py`). Isi: ringkasan hari itu + 7 hari, grafik 4 minggu, rekap bulanan, statistik backtest (CI + net), TP2/risiko, feedback loop, **hasil rekomendasi per hari (log EOD tracking.json, 7 hari terakhir + detail hari terbaru)**, event 7 hari ke depan, berita.
  - `backfill.yml` — dispatch only (jalankan manual saja; idempotent, merging)
- Rekomendasi: pola H1 di 2 bar terakhir searah trend H4 (EMA20/50) → status entry/tunggu/netral; confidence = win-rate pola **per arah bila ada** (`win_rate_long/short`) else OOS; blackout −2h..+3h sekitar event 3★ (total 5 jam per event; pasca-event 1h → 3h tervalidasi arsip event 2026-09-20); level Entry=close candle H1 terakhir yang SUDAH selesai (bar partial dibuang `recommend._closed_only`, audit 2026-09-12 — sync memang menyimpan candle yang sedang berjalan; tanpa ini pola bisa "batal" di menit akhir & level entry cuma snapshot live ≠ asumsi backtest), SL 1×ATR, TP1 1,5×ATR (**TP1-only — TP2 3×ATR dihapus 2026-09-20**); timeout posisi 24 bar H1 (24 jam). **Gate kualitas (2026-09-10, tahap 1 moderat)**: pola searah trend pun TIDAK otomatis entry — harus lolos `recommend._gate_check`: n ≥ 30 (bukti bermakna) DAN EV per sinyal net-of-cost > 0 (EV = rata-rata R termasuk timeout; fail-closed tanpa statistik). **Gate SADAR-ARAH (2026-09-12, keputusan user opsi 2+3 untuk target harian)**: EV dinilai PER ARAH sinyal dari kolom `*_long/*_short` patterns.json (`research._direction_map`, kolam = sinyal dedup NET of cost yang SEARAH trend H4 as-of — persis aturan live, via `research._tag_h4_alignment` di run_all tf 1h; prioritas `cost_oos_avg_r_{long,short}` → `cost_avg_r_{...}`, fallback statistik keseluruhan kalau per-arah tidak ada). Arah dengan bukti per-arah bermakna (n ≥ 30) tapi EV arahnya NEGATIF → dulu **entry EKSPERIMEN** (keputusan 2026-09-12 opsi 2+3) tapi **DIHENTIKAN 2026-09-20 (keputusan user setelah audit live 19 entry: 13 eksperimen hit-rate ~23%) — kini NETRAL fail-closed**, tidak pernah jadi posisi (lihat batch audit 2026-09-20 di atas; label/badge eksperimen di UI & `tracking.stats["experiment"]` dipertahankan untuk histori 13 entry lama). Eksperimen dicatat feedback loop normal tapi **statistiknya dipisah** di `tracking.stats["experiment"]` (`compute_stats`: hit-rate utama TIDAK memuat entry eksperimen; flag ikut ke `eod` entries + ledger Riwayat UI + tile "Eksperimen" di result strip). Tanpa bukti per-arah (kolom *_long/*_short tidak ada) → tetap fail-closed netral, BUKAN eksperimen. Efek di data produksi 3 tahun (H1, searah H4, net): **lolos gate**: inside_bar long (+0.122R full/+0.033R OOS, n=672), bullish_engulfing long (+0.054R/+0.091R OOS, n=287 — tadinya tersaring oleh EV keseluruhan −0.021R, asimetri arah menyelamatkannya); **eksperimen**: inside_bar short (−0.133R/−0.05R OOS, n=393), bearish_engulfing short (−0.187R/−0.146R, n=134); n<30 (shooting_star/hammer) tetap tersaring. Pola tersaring → status netral + `rec["gate"]` + rationale "disaring gate kualitas" + slot rec_log dapat field `note` (tampil di panel Riwayat). `cost_avg_r`/`cost_oos_avg_r`/`safe_cost_avg_r` diisi research `_rule_map`. Tahap 2 (calon batch berikutnya): perluas kolam setup (pullback EMA20, pin bar, morning/evening star, outside bar, dsb. — semua kandidat yang diukur 2026-09-12 GAGAL OOS net, jadi jangan ditambah tanpa bukti baru) lalu gate diperketat ke OOS. **Mode aman** (TP 1×ATR / SL 0,75×ATR, entry sama): `levels_safe` + `confidence_safe` — win-rate-nya di-backtest terpisah (`run_all` research, `safe_*` di patterns.json), feedback loop tetap menilai level standar. Layer research pakai sinyal **dedup cooldown 2 bar** (4 bar sebelum 2026-09-21, batch 4), win-rate dibubuhi **Wilson CI 95%** (`win_rate_lo/hi`), dan ada pasangan kolom **net-of-cost** (`cost_*`/`safe_cost_*`, spread $0.35/oz — barrier TP lebih jauh + SL lebih dekat dalam harga mid, R nominal tetap); rekomendasi live tetap GROSS demi kompatibilitas feedback loop, angka net tampil di rationale.
- Feedback loop: `analyzer/track.py` resolve rec aktif vs bar H1 nyata (SL dicek dulu, konservatif; timeout 24 bar) → hit-rate jujur di `data/tracking.json`. Saat resolve juga menulis `outcome` (bars_held, MFE/MAE USD, event 3★ di jendela, narasi Indonesia "mengapa benar/salah") + log akhir hari `eod` per tanggal WIB (diturunkan penuh dari history — idempotent; **hari tanpa entry ditandai `status:"skip"`, hari dengan entry masih berjalan `"run"`** — riwayat harian tanpa gap), tampil di panel Rule monitor UI.

## Frontend produksi: folder `web/` (GoldPulse)
- React 19 + Vite 7 + recharts + lucide-react, desain dari prototipe user di `gold-ui/` (Manus — **di-gitignore, jangan dipublish**).
- Baca data dari `https://raw.githubusercontent.com/ponimazino/gold/main/data/*.json` (10 file: 8 lama + `push_state.json` + `rec_log.json` + `events_hist.json`) + `data/reports/index.json` (laporan harian) + spot live `https://xaus.com/api/v1/spot?compact=1` (field `xau.price`, tanpa key, display only).
- **Web Push PWA**: `web/public/sw.js` (v4) punya listener `push` (notif dari payload JSON job daily) + `notificationclick` (buka/fokus PWA ke hash route `#/analysis` / `#/calendar` — deep-link via `location.hash` di Home.tsx). Panel langganan "Notifikasi Android" di view Jadwal (`PushPanel` Home.tsx): aktifkan izin + `pushManager.subscribe` (public key VAPID `VAPID_PUBLIC_KEY` di data.ts), tampilkan JSON subscription untuk disalin → paste ke GitHub Secrets `PUSH_SUBSCRIPTIONS`; banner "notif putus" di notice bar kalau `push_state.last_status == "expired"`.
- Struktur: `web/src/data.ts` (layer data + tipe), `web/src/pages/Home.tsx` (7 view: overview / **ringkasan** ["Apa kata hari ini", bahasa awam] / analysis / backtest [ada panel PDF harian — laporan terbaru tampil inline tanpa klik, arsip klik → popup modal] / **riwayat** [panel per hari DEFAULT HARI INI WIB: slot analisa tiap ±2 jam dari `rec_log.json` (jam + status Entry/Tunggu/Skip, hasil entry dicocokkan ke tracking via tanggal+jam; pilih tanggal lain) + ledger history per entry: level entry/SL/TP + hasil + narasi, filter hasil & pola, hari tanpa entry = baris skip] / calendar / jadwal), `web/src/index.css` (design system goldpulse), PWA di `web/public/` (manifest+sw.js — navigasi network-first, asset cache-first).
- Chart Primary Instrument: bisa **digeser (drag) & zoom (scroll + tombol +/−/reset)** — window 15..1000 bar dari 1200 bar dimuat; default 90 bar terakhir.
- Build: `npm run check` (tsc) + `npm run build`. Bundle ~188 kB gzip.
- **PWA vanilla lama masih ada di root repo** (index.html/app.js/style.css/sw.js/manifest.webmanifest) — tidak ter-deploy saat Vercel root=web, tapi kandidat pembersihan (tanya user dulu).

## Next step user: deploy ke Vercel
Root Directory = `web` (auto-detect Vite), lihat DEPLOY.md Bagian 5. Setelah itu: verifikasi workflow `daily.yml` jalan di grid 2 jam (cek tab Actions, run pertama setelah push di jam :37 WIB berikutnya).

## Batch TER-PUSH `5ae02a8` (2026-09-12 malam): gate sadar-arah + eksperimen EV-negatif — SUDAH LIVE (dispatch run 34702021680, iterasi pertama commit `477b53d`; patterns.json produksi terverifikasi punya kolom per arah; sisa user: redeploy Vercel)
- Keputusan user (opsi 2+3, target frekuensi harian): short tidak diblokir — dijalankan sebagai **EKSPERIMEN** berlabel jelas & dinilai terpisah; review saat n live eksperimen ≥ 30.
- Analisa pendukung (data 3 tahun, aturan produksi): asimetri arah NYATA (inside_bar long +0.122R vs short −0.133R, konsisten 2024/25/26 — bull market); semua kandidat setup baru Tahap 2 (pullback_ema20 −0.019R, pin_bar OOS −0.075R, outside_bar OOS −0.256R, star n kecil) GAGAL OOS net → tidak ditambahkan; mode aman TIDAK menolong short (−0.178R vs −0.133R) → eksperimen pakai level standar; shadow signals DIBATALKAN (research tiap run sudah mengevaluasi ulang semua sinyal historis).
- Kode: `recommend.py` (`_gate_ev/_gate_n/_gate_check/_experiment_eligible` sadar-arah; loop matched split qualified/experiments; experiment path level standar + rationale jujur), `research.py` (`_tag_h4_alignment` — trend H4 as-of per sinyal H1, aturan live; `_direction_map` — kolom `n_long/win_rate_long/cost_avg_r_long/cost_oos_avg_r_long/+_short` dari kolam searah H4), `track.py` (compute_stats pisah `stats["experiment"]`; build_eod bawa flag), `reclog.py` (slot `experiment`), `push.py` (notif ⚠ EKSPERIMEN), `data.ts`+`Home.tsx` (badge EKSPERIMEN di Analysis+Riwayat, tile Eksperimen, EV per arah di panel Backtest).
- Tes: test_p5 +`test_direction_gate_and_experiment` (long lolos via EV per-arah meski EV total negatif; short EV-negatif n cukup → eksperimen level standar; tanpa bukti per-arah → netral fail-closed) +`test_stats_experiment_split`; reason gate kini sebut arah ("n long=4"). 36/36 Python + tsc + build lulus (201 kB gzip). E2E data produksi via `scripts/verify_gate.py` (LOKAL jangan di-commit): angka per arah match eksplorasi; momen live 2026-09-12 → inside_bar short H4-down = EKSPERIMEN entry.

## Batch TER-PUSH `83909fe` + audit 2026-09-12 (batch bar partial + grid 1 jam — SUDAH DI-PUSH, lihat git log untuk hash final)
- **Fix bar partial (temuan audit 2026-09-12)**: sync menyimpan candle yang sedang berjalan; analisa pernah memakainya (bukti: entry 10 Sep 4362.09 ≠ close bar manapun; entry 11 Sep 4317.42 vs close final 4317.24). Fix `recommend._closed_only(bars, step_h, now)` — buang bar terakhir yang belum genap umur step_h sebelum analisa H1/H4. Efek: sinyal & level 100% dari candle closed, konsisten backtest ↔ live.
- **Grid analisa 2 jam → 1 jam**: daily.yml "37 0-15 * * 1-5" + "37 21-23 * * 0-4" + cadangan :52, watch loop interval 2h → 1h. Kuota ~38 dari 800 kredit/day. Tiap candle H1 baru dievaluasi ≤ 37 menit setelah close. TIDAK menaikkan jumlah setup (gate tetap) — hanya kecepatan tangkap sinyal.
- **Fix `stats.active`**: `compute_stats` kini menghitung status "entry" + "active" (dulu tile UI "Berjalan" tampil 0 padahal ada posisi entry aktif).
- **Label jujur Riwayat**: slot entry yang tidak jadi posisi diberi `note` "sinyal saat posisi lain masih berjalan — tidak dicatat/dinilai feedback loop (aturan 1 posisi aktif)" (`reclog.log_run(entry_recorded=...)`); frontend fallback text slot tak-cocok diubah dari "Level dicatat — hasil mulai dinilai..." (menyesatkan) ke note/fallback jujur.
- Tes: test_p5 `_seed` bar kini berakhir di masa lalu (closed) + `test_partial_bar_dropped` (H1+H4) + `test_stats_counts_entry_as_active`; test_reclog + `test_entry_note_when_not_recorded`. 31+3 Python lulus, tsc + build lulus (201 kB gzip).
- **Usulan user-approved menunggu batch berikutnya**: (a) Tahap 2 perluas kolam setup (pullback EMA20, pin bar, morning/evening star, outside bar) lalu gate diperketat ke EV OOS; (b) *shadow signals* — semua sinyal (termasuk yang tidak jadi posisi) dievaluasi diam-diam vs harga untuk mempercepat validasi pola, terpisah dari statistik posisi nyata.

## Batch `0b66a89` (2026-09-09 malam): laporan mingguan → harian EOD

- `report.yml` cron "7 16 * * 1-5" + cadangan "53 16" (23:07 WIB Sen–Jum, setelah grid daily terakhir); `report.py` → `daily-YYYY-MM-DD.pdf`, banner/judul "LAPORAN HARIAN", KEEP 52→90, ringkasan eksekutif kini OHLC hari itu (WIB) + 7 hari, **section 7 baru "Hasil rekomendasi per hari"** (tabel 7 hari dari log EOD tracking.json + detail entry hari terbaru: pola/bias/hasil/MFE-MAE/narasi), section event → "7 hari ke depan" (no. 8), berita → no. 9. Frontend: label panel Backtest + kartu Jadwal → "Laporan harian PDF (EOD)". Smoke test + pypdf + tsc + build LULUS. PDF mingguan lama tetap di repo (arsip, index tetap valid).

## Batch TER-PUSH `9b8689b` (2026-09-09): upgrade jujur backtest + layout Riwayat mobile
- **Upgrade jujur backtest** (assessment vs literatur: Bailey/López de Prado dsb.), 4 perbaikan sekaligus di layer research (`analyzer/jobs/research.py`, `analyzer/backtest.py`):
  - **Net of cost**: `cost_usd=0.35` (spread standar XAUUSD) menggeser barrier MELAWAN trader dalam harga mid — TP harus tercapai +$0.35 lebih jauh, SL kena $0.35 lebih cepat; R nominal per trade TIDAK berubah. Kolom `cost_win_rate`/`cost_oos_win_rate` + `safe_cost_*` di patterns.json. Rekomendasi live masih pakai angka GROSS (kompatibel feedback loop); angka net tampil di rationale + panel Backtest UI.
  - **Dedup sinyal overlap**: sinyal searah dalam 4 bar dari sinyal searah sebelumnya di-drop di layer research (rekomendasi live TIDAK didedup). n jujur turun drastis (inside_bar H1: 2806 → 1542).
  - **Wilson CI 95%**: `win_rate_lo/hi` di patterns.json + tampil di panel Backtest; confidence_note rekomendasi menyebut rentangnya.
  - **TP2 runner + risiko di PDF mingguan** (`report.py` §5b): skenario TP1 → SL breakeven → kejar TP2 3×ATR (outcome tp2/be/stopped/no_tp1/timeout) + `risk_stats` (max drawdown R, loss streak) per pola H1.
  - `research.run_pair` → `research.run_all` (return 4 list: std/safe/std-net/safe-net); test_p3 +4 tes (cost/CI/dedup/tp2+risk); 5/5 suite + report smoke + tsc + build LULUS.
  - BUG FIX saat implementasi: model cost awal TERBALIK arah SL (SL digeser lebih jauh = terlalu optimis) — dikoreksi jadi SL lebih dekat; `report.py` loop var `pat` men-shadow alias modul `patterns` → rename `pname`.
  - **Fix layout view Riwayat mobile**: level dipecah jadi chip per-level (Entry/SL/TP1/TP2 terpisah, `.hist-detail span` = chip mono + warna good/bad), media ≤760px pill status turun ke baris sendiri (`.hist-ledger` 2 kolom) — teks tidak lagi terdesak. Terverifikasi playwright-core (channel msedge) 390px: 0 overflow / 0 overlap / 0 chip terpotong.
  - Push: rebase di atas auto-commit workflow (285d269), konflik `data/patterns.json` resolve `--theirs` (versi lokal), autostash untuk docs/ yang tidak di-commit.
- **Watch loop anti-drop + view Riwayat** (batch 2026-09-08 malam): SUDAH di-push `f89d62f`. Batch sebelum itu (grid 2 jam, EOD, mode aman, cron cadangan): `35d016d` + `abf57cd`.
- Dispatch manual: **cukup daily.yml** — run baru mem-cancel loop lama, sync sendiri dulu, lalu analisa pakai kode baru. sync/fundamental/report tidak perlu dispatch.

## Audit 2026-09-06 — bug yang sudah diperbaiki (jangan kambuh)
- `track.py`: job daily mencatat status "entry" tapi `_resolve_one` cuma proses "active" → rekomendasi tak pernah dinilai. FIXED: normalisasi entry→active (regression test di test_p5.py). Hit-rate baru muncul setelah rekomendasi mulai teresolve (24 bar H1 ~1 hari).
- `web/public/sw.js`: index.html cache-first bikin user stuck versi lama setelah deploy. FIXED: request navigasi = network-first.
- localStorage (analyst notes) dibungkus try/catch (private mode).
- Fundamentals: event >24 jam lampau difilter dari feed.

## Gotcha Windows/Git yang pernah terjadi
- Credential Manager pernah cache akun salah (miawopen) → 403. Pakai remote URL dengan hint username `https://ponimazino@github.com/...`.
- Push dari shell Claude pernah gagal "Cannot prompt / could not read Username" → kalau git butuh interaksi auth, minta user push sendiri.

## Gotcha GitHub Actions (terverifikasi 2026-09-08)
- **GitHub sering MENUNDA/DROP run terjadwal** — bukan jitter 10 menit: 2026-09-08 Hourly Sync cuma jalan 2×/hari (dari 48 slot), grid daily 10 slot jalan 0×. Slot cron cadangan menit-menit TIDAK cukup (yang di-drop jamnya juga). Solusi definitif: **watch loop** di sync.yml + daily.yml — run yang lolos tetap hidup ~6 jam dan bekerja sendiri secara berkala (sync tiap 30 mnt; daily: sync+analisa tiap 2 jam). Asal 3–4 run/hari lolos, data & rekomendasi segar terus. Job idempotent + `cancel-in-progress: true` jadi dobel-run aman dan kuota API terkendali.
- `data/*.json` konflik saat `git pull --rebase` (auto-commit workflow remote vs commit lokal kita): saat rebase, versi commit lokal = `--theirs`.